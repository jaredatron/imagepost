#!/usr/bin/env ruby

ENV['BUNDLE_GEMFILE'] = File.expand_path('../../Gemfile', __FILE__)
require 'rubygems'
require 'bundler'

require File.expand_path('../../environment', __FILE__)

if defined?(Pry)
  Pry.start(ImagePost)
else
  require 'irb'
  IRB.start
end
